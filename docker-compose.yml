services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - picker-data:/data
    networks: [picker]

networks:
  picker:

volumes:
  picker-data:
